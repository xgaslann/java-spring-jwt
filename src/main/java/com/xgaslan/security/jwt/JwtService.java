package com.xgaslan.security.jwt;

import org.springframework.stereotype.Component;

@Component
public class JwtService {

    // This class can be used to handle JWT operations such as token generation, validation, etc.
    // For now, it is empty and can be implemented later as per requirements.

    // Example methods could include:
    // - generateToken(String username)
    // - validateToken(String token)
    // - getUsernameFromToken(String token)
}
